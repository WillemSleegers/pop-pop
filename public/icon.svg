<svg width="512" height="512" viewBox="0 0 512 512" fill="none" xmlns="http://www.w3.org/2000/svg">
  <!-- Background -->
  <rect width="512" height="512" rx="115" fill="#7C3AED"/>

  <!-- Large circle (back) -->
  <circle cx="320" cy="320" r="120" fill="#A78BFA" opacity="0.8"/>

  <!-- Medium circle (middle) -->
  <circle cx="200" cy="240" r="90" fill="#C4B5FD" opacity="0.9"/>

  <!-- Small circle (front) -->
  <circle cx="280" cy="180" r="70" fill="#E9D5FF"/>

  <!-- Highlight effect on small circle -->
  <circle cx="260" cy="160" r="25" fill="white" opacity="0.5"/>
</svg>
